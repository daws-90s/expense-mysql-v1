[mysqld]
# General query log stays off deliberately — it logs every single query,
# which is extremely high volume and mostly noise for this lab.
slow_query_log = 1
slow_query_log_file = /var/log/mysql/slow.log
long_query_time = 1
